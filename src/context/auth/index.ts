/**
 * Authentication context exports
 * Provides a simplified import pattern for authentication features
 */

export { AuthProvider, useAuth } from './AuthContext';
export type { AuthContextType } from '../../types/auth';
